# 🏗 `tanzubuilder`

```shell
tanzubuilder init
```

## 🚧 todo
* [ ] PoC a plugin that `init`s a project with `reconciler-runtime`
* [x] bootstrap e2e tests w/ `ginkgo`
* [ ] `goreleaser` & `brew`
